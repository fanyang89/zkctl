[package]
name = "zkctl"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
tokio = { version = "1.44", features = ["macros", "rt-multi-thread"] }
zookeeper-client = { version = "0.11.1", features = ["tokio"] }
